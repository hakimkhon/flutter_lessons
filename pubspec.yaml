name: lessons_cody
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
roduct and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'
 versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

     with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  gap: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # es and activating additional ones.
  flutter_lints: ^3.0.0

flutter:

  # The folrial Icons class.
  uses-material-design: true
  assets:
    - assets/images
